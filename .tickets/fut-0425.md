---
id: fut-0425
status: closed
deps: [fut-87vl]
links: [fut-m59z]
created: 2026-08-07T09:57:56Z
type: feature
priority: 2
assignee: Mikkel Malmberg
---
# Companion package: opinionated worktree/workflow bundle on top of fut

Fut stays naive: mechanism, not policy. A separate package builds on the plain CLI and the event stream to ship opinions — auto-opening git worktrees as workspaces (via git hooks or discovery), project conventions, default tab setups, and whatever else fits the everything-works-together bundle. Fut never learns what a worktree is; the companion owns all of it. Precedent: git plumbing/porcelain, tmux vs tmuxinator.


## Notes

**2026-08-16T06:25:53Z**

Updated the bundled wt extension to ask only for the worktree name, launch pi without an initial prompt, and accept the post-open action as direct argv configured in fut-extension.toml. Updated docs, changelog, and e2e coverage. Validation: shell syntax, focused e2e test, cargo fmt --check, cargo build.

**2026-08-16T18:28:36Z**

Finished the bundled companion workflow: wt now asks only for a worktree name, runs a configurable prompt-free post-open action, and opts into exact parent-client activation of the target returned by fut open. Extension commands gained configurable popup sizing and qualified palette slugs; the website now has a dedicated extension API guide. Refactor pass consolidated shared command sizing/activation configuration and replaced truncated random socket names with short process-local monotonic names. Validation: 448 unit tests, focused activation e2e, Jekyll build, clippy all targets, cargo build.
